server:
  port: 9004

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

  datasource:
    url: jdbc:mysql://localhost:3306/BookingDB
    username: root
    password: